# 功能使用详解 / Feature Guide

详细了解网站各项功能的使用方法。

## 📑 目录

- [首页板块](#首页板块)
- [博客系统](#博客系统)
- [内容配置](#内容配置)
- [自定义样式](#自定义样式)

---

## 🏠 首页板块

网站首页包含以下板块，可按需修改或隐藏。

### 修改板块内容

每个板块对应 `contents/` 目录下的 `.md` 文件：

| 板块 | 文件 | 说明 |
|------|------|------|
| 首页 | `home.md` | 个人简介、教育背景、联系方式 |
| 新闻 | `news.md` | 最新动态、通知公告 |
| 奖项 | `awards.md` | 获奖荣誉、成就 |
| 经历 | `experience.md` | 工作经历、项目经验 |
|  publications | `publications.md` | 论文列表、项目展示 |

### 示例：编辑 `home.md`

```markdown
# 个人简介标题（可选，会被 config.yml 覆盖）

我是 XXX，目前在 XXX 攻读博士学位。

我的研究方向包括 XXX、XXX 和 XXX。

#### 研究兴趣
- 人工智能
- 机器学习
- 计算机视觉

#### 联系方式
- Email: your@email.com
- GitHub: [yourusername](https://github.com/yourusername)
```

---

## 📝 博客系统

博客系统是完全独立的，可以单独使用，也可以与首页配合。

### 访问博客

- 导航栏点击 **BLOG** 链接
- 或直接访问 `/blog.html`

### 博客功能

- **分类筛选**：左侧边栏可按分类筛选文章
- **标签搜索**：输入标签名称实时搜索
- **排序选择**：按日期或标题排序
- **文章阅读**：点击文章卡片进入详情页

### 添加新文章

#### 1. 创建文章文件

在 `contents/blog/` 目录下创建 `.md` 文件：

```markdown
---
# 不需要 front matter，直接写内容
---

# 文章标题

文章正文内容...

## 二级标题

- 列表项 1
- 列表项 2

\`\`\`python
# 代码示例
def hello():
    print("Hello, World!")
\`\`\`

### 数学公式

行内公式：$E = mc^2$

块级公式：
$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$
```

#### 2. 添加文章索引

编辑 `contents/blog/articles.yml`，添加：

```yaml
- id: "unique-article-id"      # 唯一标识，用于 URL
  title: "文章标题"             # 显示标题
  date: "2025-01-25"           # 日期 YYYY-MM-DD
  category: "分类名"            # 分类
  tags: ["标签1", "标签2"]      # 标签列表
  summary: "文章摘要..."        # 简介，显示在列表页
  file: "2025-01-25-filename.md"  # 对应的文件名
```

### 支持的 Markdown 语法

| 功能 | 语法 | 示例 |
|------|------|------|
| 标题 | `#` `##` `###` | `# 一级标题` |
| 粗体 | `**text**` | **粗体** |
| 斜体 | `*text*` | *斜体* |
| 代码 | `` `code` `` | `code` |
| 代码块 | ``` ``` | 多行代码 |
| 链接 | `[text](url)` | [链接](url) |
| 图片 | `![alt](url)` | ![图片](url) |
| 列表 | `- item` | • 项目 |
| 引用 | `> text` | > 引用 |
| 表格 | 见下表 | |

### 数学公式支持

使用 MathJax 渲染，支持 LaTeX 语法：

**行内公式**：`$公式$`
- 示例：$E = mc^2$，$\sin(x)$

**块级公式**：`$$公式$$`
- 示例：
$$
\int_a^b f(x) dx = F(b) - F(a)
$$

**常用符号**：
- 希腊字母：`$\alpha$, $\beta$, $\pi$`
- 运算符：`$\sum$, $\prod$, $\int$`
- 关系符：`$\leq$, $\geq$, $\approx$`

---

## ⚙️ 内容配置

### config.yml 完整配置

```yaml
# 基本设置
title: 你的名字 - 个人主页      # 浏览器标签页标题
page-top-title: 你的名字        # 导航栏显示
top-section-bg-text: 你的人生格言  # 首页大图文字

# 首页副标题
home-subtitle: 你的职位 | 你的学校/公司

# 版权信息
copyright-text: '&copy; 你的名字 2023-2026. All Rights Reserved.'

# GitHub 链接（可选）
github-username: 你的用户名
```

### 图片路径

所有图片路径相对于 `index.html`，使用 `static/` 前缀：

```markdown
![头像](static/assets/img/photo.png)
![背景](static/assets/img/background.jpeg)
```

---

## 🎨 自定义样式

### 修改颜色

编辑 `static/css/main.css` 中的 `:root` 变量：

```css
:root {
    --h-title-color: #3948d2;  /* 主色调 */
}
```

### 添加自定义 CSS

在 `static/css/main.css` 末尾添加：

```css
/* 你的自定义样式 */
.my-custom-class {
    color: red;
}
```

### 修改布局

- 板块顺序：调整 `index.html` 中 `<section>` 的顺序
- 隐藏板块：删除对应的 `<section>` 或 `li` 元素
- 添加板块：参考现有板块结构复制修改

---

## 📱 响应式设计

网站已内置响应式支持：

- 桌面端（>991px）：完整布局
- 平板端（768-991px）：自适应
- 手机端（<768px）：单栏布局

无需额外配置，Bootstrap 自动处理。

---

## ➡️ 进阶

想进行更深入的自定义？参考 [ADVANCED.md](ADVANCED.md)
